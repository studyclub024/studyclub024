rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY SEED RULES - ALLOWS ALL WRITES FOR SEEDING
    // DEPLOY THIS, RUN SEED, THEN DEPLOY PRODUCTION RULES IMMEDIATELY
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
